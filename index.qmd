---
title: "tatami"
author: "Christoph Scheuch"
format: dashboard
server: shiny
brand: inst/_brand.yml
css: inst/assets/css/custom.css
---

```{r}
#| context: setup
pkgload::load_all()

config <- get_config()
```

```{r}
#| context: data
context_data()
```

# {.sidebar}

```{r}
clusters_input(input)
```

# Plot

```{r}
clusters_output("plot", output)
```

# Table

```{r}
clusters_output("table", output)
```

# Summary

## Row

```{r}
summary_output("summary_table", output)
```

## Row

```{r}
summary_output("summary_stats", output)
```

```{r}
#| context: server
clusters_server(input, output)
summary_server(input, output)
```
